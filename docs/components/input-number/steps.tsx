import { ElInputNumber } from '@qsxy/element-plus-react';
import React from 'react';

const App = () => {
    return <ElInputNumber defaultValue={5} step={2} style={{ width: 200 }} />;
};

export default App;
